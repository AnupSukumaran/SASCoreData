import XCTest
@testable import SASCoreData

final class SASCoreDataTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SASCoreData().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
